export class FileType {
	/**Prefix for each file type used */
	Prefix: string;

	/**Specifies whether the file is an application. */
	IsApplication: boolean;

	/**Storage area category */
	StorageArea: string;
}
